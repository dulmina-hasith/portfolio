{
  "name": "portfolio-build-scripts",
  "private": true,
  "type": "module",
  "description": "Build-time prerendering for SEO — not shipped to the browser",
  "scripts": {
    "build": "node build.mjs"
  },
  "dependencies": {
    "cheerio": "^1.0.0"
  }
}
